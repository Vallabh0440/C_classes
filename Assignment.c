#include<stdio.h>
int main(){
    int a = 50;
    // a += 10;

    // printf("value of + assigment operator is %d\n",a);

    // a -= 10;
    // printf("value of - assigment operator is %d\n",a);

    // a *= 10;
    // printf("value of * assigment operator is %d\n",a);

    // a /= 10;
    // printf("value of / assigment operator is %d\n",a);

    // // a %= 20;
    // // printf("value of % assigment operator is %d\n",a);

    // a &= 10;
    // printf("value of & assigment operator is %d\n",a);

    // a |= 10;
    // printf("value of | assigment operator is %d\n",a);

    // a ^= 10;
    // printf("value of ^ assigment operator is %d\n",a);

    a <<= 5;
    printf("value of << assigment operator is %d\n",a);

    a >>= 3;
    printf("value of >> assigment operator is %d\n",a);

}